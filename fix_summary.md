- **마우스 휠 & 핀치투줌 지원:** React의 `onTouchStart`/`Move`/`End` 이벤트를 통해 두 손가락 핀치 줌을 완벽하게 지원합니다. 또한 `onWheel` 이벤트를 최적화하여 휠을 굴릴 때 스크롤이 움직이지 않고 확대/축소만 작동하도록 개선했습니다.
- **부드러운 사진 이동 최적화:** 이미지 드래그 및 확대/축소 시 브라우저 병목(렉)을 없애기 위해 `requestAnimationFrame`을 적용해 최적화된 하드웨어 가속 트랜지션을 구현했습니다.
- **정확한 사이즈 가이드라인 렌더링:** 둥근 사각형(`rounded-xl`)의 썸네일 비율과 정확히 일치하는(33.3% 곡률 적용) 크롭 박스를 구현했으며, 크롭 영역 밖은 반투명한 어두운 레이어로 덮이도록 `box-shadow` 트릭을 적용하여 직관성을 높였습니다.
- **모바일 스크롤 및 팝업 사이즈 최적화:** 모바일에서 세로로 잘리지 않도록 팝업의 전체 길이를 압축하고, 이미지 이동 중에는 터치 스크롤 충돌을 막기위해 `touch-action: none`을 적용했습니다. 제보하기 버튼 또한 요청에 따라 숨김 처리했습니다.
