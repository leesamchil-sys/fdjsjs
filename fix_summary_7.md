- **프로필 이미지 클라우드 마이그레이션 밑작업 완료:** 현재 로컬 환경(IndexedDB)에 저장된 사용자들의 프로필 이미지를 추후 **Firebase Storage** 등 외부 클라우드로 손실 없이 안전하게 이전할 수 있도록 기반 작업을 진행했습니다.
  - `src/utils/indexedDB.ts`에 로컬에 저장된 모든 이미지 Blob 데이터를 한 번에 추출하는 `getAllLocalPetImages` 유틸리티 함수를 추가했습니다.
  - 특별한 아키텍처 변경 없이도 현재 구조(`Pet` 객체의 `hasCustomImage` 플래그 및 IndexedDB ID 매핑)를 그대로 활용하여, 원할 때 언제든 **사용자가 설정해 둔 이미지를 유지한 상태로 즉시 마이그레이션이 가능함을 확인**했습니다.
